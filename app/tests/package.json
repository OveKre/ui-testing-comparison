{
  "name": "ui-testing-comparison",
  "version": "1.0.0",
  "description": "UI testimise raamistike võrdlus - Cypress vs Playwright",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:headless": "cypress run --headless",
    "playwright:test": "playwright test",
    "playwright:test:headed": "playwright test --headed",
    "playwright:test:debug": "playwright test --debug",
    "playwright:report": "playwright show-report",
    "test:cypress": "npm run cypress:run",
    "test:playwright": "npm run playwright:test",
    "test:all": "npm run test:cypress && npm run test:playwright",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "cypress": "^13.6.0",
    "playwright": "^1.40.0",
    "@playwright/test": "^1.40.0"
  },
  "keywords": [
    "testing",
    "ui-testing",
    "cypress",
    "playwright",
    "e2e",
    "automation"
  ],
  "author": "TAK24",
  "license": "ISC"
}